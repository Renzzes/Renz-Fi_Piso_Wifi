# Generated Admin + portal payload

Run `npm run build:openwrt` from the repository root. That command writes
`index.html`, `assets/`, `portal/`, and `build-info.json` here.

Do not hand-edit generated files. Sources live in `src/` and `portal/`.
