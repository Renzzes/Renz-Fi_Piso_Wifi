/**
 * MikroTik voucher adapter boundary (stub).
 * Real voucher binding and RouterOS scripting can be implemented later.
 */
export async function syncVouchersFromRouter() {
  return { ok: true };
}
